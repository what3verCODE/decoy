# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).
Run `pnpm changeset` to record a version bump + changelog entry for the `@decoy/*`
packages you touched. CI consumes these on release.
