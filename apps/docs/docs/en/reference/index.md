---
title: Reference
description: Exhaustive lookup — the configuration schema, the control / HTTP API, and the generated TypeScript API.
---

# Reference

Exhaustive lookup, distinct from the task-first [guides](/guide/start/introduction):

- **[Configuration](/reference/configuration)** — every `defineConfig` option and mock-file field,
  with types, defaults, and constraints.
- **[Control / HTTP API](/reference/control-api)** — the in-process Controller, the
  transport-agnostic Router and clients, and every `/__decoy__` HTTP endpoint.
- **[API](/reference/api/)** — the generated TypeScript reference for every `@decoy/*` package,
  emitted from source JSDoc so it never drifts.
