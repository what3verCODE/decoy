id: users-by-id
method: GET
path: /users/{id}
presets:
  default: {}
variants:
  ada:
    status: 200
    body:
      id: 42
      name: Ada
  boom:
    status: 500
    body:
      error: upstream exploded
