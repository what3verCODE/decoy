{
  "name": "@decoy/fastify",
  "version": "0.0.0",
  "description": "Decoy Fastify plugin: embed the in-process engine for partial mocking — matched routes are served from mocks, unmatched requests fall through to the host app (fail closed when none answers).",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "publishConfig": {
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "import": "./dist/index.js",
        "require": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rslib build",
    "test": "rstest run",
    "typecheck": "tsc -p tsconfig.lint.json"
  },
  "dependencies": {
    "@decoy/config": "workspace:*",
    "@decoy/core": "workspace:*"
  },
  "peerDependencies": {
    "fastify": "^4 || ^5"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/supertest": "^6.0.0",
    "fastify": "^5.0.0",
    "supertest": "^7.0.0"
  }
}
