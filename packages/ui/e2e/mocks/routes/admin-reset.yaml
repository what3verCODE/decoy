# POST /__decoy__/reset — drop all overrides back to the active collection baseline.
id: admin-reset
method: POST
path: /__decoy__/reset
presets:
  default: {}
variants:
  cleared:
    status: 200
    body:
      collection: happy-path
      overrides: []
